# Part 2. 연산자

> 산술·비교·논리 세 가지. 반복문·조건문 안에 자주 등장하는 도구.

---

## 2.1 산술 연산자

| 연산자 | 의미 | 예시 | 결과 |
|---|---|---|---|
| `+` | 더하기 | `7 + 3` | `10` |
| `-` | 빼기 | `7 - 3` | `4` |
| `*` | 곱하기 | `7 * 3` | `21` |
| `/` | 나누기 (실수) | `7 / 3` | `2.333...` |
| `//` | 몫 (정수 나누기) | `7 // 3` | `2` |
| `%` | 나머지 | `7 % 3` | `1` |
| `**` | 거듭제곱 | `2 ** 3` | `8` |

**`//`와 `%` 활용 예**

자릿수 분리:
```python
n = 1234
print(n // 1000)        # 천의 자리
print(n // 100 % 10)    # 백의 자리
print(n // 10 % 10)     # 십의 자리
print(n % 10)           # 일의 자리
```

```
1
2
3
4
```

짝홀 판별의 기본:
```python
print(10 % 2)
print(7 % 2)
```

```
0
1
```

### 헷갈리기 쉬움: `/` vs `//`

```python
print(7 / 2)
print(7 // 2)
print(-7 // 2)
```

```
3.5
3
-4
```

> `/`는 항상 실수, `//`는 몫. 음수에서 `//`는 더 작은 정수 쪽으로 내려간다.

---

## 2.2 비교 연산자 → 결과는 항상 `True` 또는 `False`

| 연산자 | 의미 |
|---|---|
| `==` | 같다 |
| `!=` | 다르다 |
| `>`, `<` | 크다, 작다 |
| `>=`, `<=` | 크거나 같다, 작거나 같다 |

```python
print(5 > 3)
print(5 == 5)
print(5 == "5")
```

```
True
True
False
```

> 자료형이 다르면 같지 않다고 본다.

### 주의: `=` vs `==`

- `=` (단일): **대입** (값을 변수에 저장)
- `==` (이중): **비교** (같은지 확인, 결과는 True/False)

```python
x = 10
print(x == 10)
```

```
True
```

---

## 2.3 논리 연산자

| 연산자 | 의미 |
|---|---|
| `and` | 둘 다 참일 때 참 |
| `or` | 하나라도 참이면 참 |
| `not` | 반대로 |

```python
age = 17
print(age >= 13 and age <= 19)
print(age < 13 or age > 19)
print(not True)
```

```
True
False
False
```

### 진리표

| A | B | A and B | A or B | not A |
|---|---|---|---|---|
| T | T | T | T | F |
| T | F | F | T | F |
| F | T | F | T | T |
| F | F | F | F | T |

---

## 2.4 🌟 [고급] 연산자 우선순위

수학과 거의 같다고 보면 됨:
1. 거듭제곱 `**`
2. 곱셈/나눗셈 `*`, `/`, `//`, `%`
3. 덧셈/뺄셈 `+`, `-`
4. 비교 `==`, `>`, `<` 등
5. 논리 `not`
6. 논리 `and`
7. 논리 `or`

```python
print(2 + 3 * 4)
print((2 + 3) * 4)
print(10 % 3 + 1)
```

```
14
20
2
```

> 헷갈릴 땐 **괄호**로 묶자. 가독성도 좋아진다.

---

## 2.5 자주 빠지는 함정 ⚠️

1. **`/`는 항상 실수**, 정수 나누기는 `//`
2. **`=`는 대입, `==`는 비교** — `if x = 5:`는 SyntaxError
3. **자료형 다르면 같지 않음** — `5 == "5"`는 `False`
4. **`and`, `or`, `not`은 소문자**
5. **0, 빈 문자열, 빈 리스트는 `False`로 취급**
6. **연산 우선순위 헷갈리면 괄호 사용**

---

## 2.6 패턴

### 패턴 1. 자릿수 분리 (4자리 정수)

```python
n = 1234
print(n // 1000)
print(n // 100 % 10)
print(n // 10 % 10)
print(n % 10)
```

```
1
2
3
4
```

### 패턴 2. 짝홀 판별

```python
print(10 % 2 == 0)
print(7 % 2 == 0)
```

```
True
False
```

### 패턴 3. 범위 검사

```python
age = 17
print(13 <= age <= 19)
print(age >= 13 and age <= 19)
```

```
True
True
```

> `13 <= age <= 19`는 파이썬 특유의 표현. 다른 언어에는 보통 없다.

---

## 2.7 연습문제

**1) 정수를 입력받아 짝수면 True, 홀수면 False 출력**

**2) 두 정수를 입력받아 `/`와 `//` 결과를 각각 출력**

**3) 정수를 입력받아 1 이상 100 이하인지 True/False 출력**

**4) 3자리 정수의 각 자리수의 합을 출력 (예: 357 → 15)**

---

## 해설

**1)**
```python
n = int(input())
print(n % 2 == 0)
```

(입력 `8`)
```
True
```

**2)**
```python
a = int(input())
b = int(input())
print(a / b)
print(a // b)
```

(입력 `10`, `3`)
```
3.3333333333333335
3
```

**3)**
```python
n = int(input())
print(1 <= n <= 100)
```

(입력 `50`)
```
True
```

**4)**
```python
n = int(input())
total = n // 100 + n // 10 % 10 + n % 10
print(total)
```

(입력 `357`)
```
15
```
