# Part 7. 시험 유형별 풀이 전략

> 시험 문제는 크게 4가지 유형. 각 유형마다 **풀이 사고 순서**가 정해져 있다.
> 막히면 이 순서를 따라가면 된다.

---

## 7.1 코드 작성형

문제를 보고 직접 파이썬 코드를 짜서 제출하는 형태.

### 5단계 사고 순서

**Step 1. 문제 읽기**
끝까지 읽고 무엇을 시키는지 파악한다.

**Step 2. 입력값과 출력값 확인**
종이/주석에 적는다.
```
입력: 정수 N (한 줄)
출력: 1부터 N까지의 합
```

**Step 3. 입력값을 `input()`으로 받기**

```python
n = int(input())
```

**Step 4. 출력값을 만드는 로직 구현**

- 단순 계산? → 그대로 식 작성
- 분기 필요? → if/else
- 반복 필요? → for/while
- 누적 합/카운트/최대? → 누적 패턴 4가지
- 데이터가 여러 개? → 리스트

```python
n = int(input())
total = 0
for i in range(1, n+1):
    total += i
```

**Step 5. `print()`로 출력**

```python
print(total)
```

전체 코드 실행 결과:

(입력 `5`)
```
15
```

### 막혔을 때 자기 점검 질문

1. "이 문제는 7유형 중 어디에 가까운가?" (Part 8 참고)
2. "지금 변수에 어떤 값이 들어있는지 print로 찍어볼까?"
3. "`int()` 빼먹은 곳 없나?"
4. "들여쓰기는 일치하나?"

---

## 7.2 코드 읽기 + 결과 예측

주어진 코드의 출력을 예측하는 문제.

### 풀이법: **변수 추적표 그리기**

```python
x = 5
y = 3
x = x + y
y = x - y
x = x - y
print(x, y)
```

```
3 5
```

**추적표**

| 줄 | x | y |
|---|---|---|
| `x = 5` | 5 | - |
| `y = 3` | 5 | 3 |
| `x = x + y` | 8 | 3 |
| `y = x - y` | 8 | 5 |
| `x = x - y` | 3 | 5 |

> 두 변수 값을 **교환**하는 코드.

### 반복문 추적

```python
total = 0
for i in range(1, 5):
    total += i
print(total)
```

```
10
```

| i | total (반복 끝난 뒤) |
|---|---|
| 1 | 1 |
| 2 | 3 |
| 3 | 6 |
| 4 | 10 |

### 팁
- 반복문은 **첫 1~2회 + 마지막 1회**만 따져보면 패턴이 보임
- 조건문은 **모든 분기마다 어디로 가는지** 표시

---

## 7.3 빈칸 채우기

코드의 일부가 비어있고, 의도된 출력이 나오게 채우는 문제.

### 풀이법: **앞뒤 흐름으로 의도 추론**

**예시**
```python
n = int(input())
total = 0
for i in range(1, n+1):
    if [        ]:
        total += i
print(total)
```

> 입력 10일 때 출력이 30이 나오게 빈칸을 채우시오.

**추론 과정**
1. n=10일 때 1~10 중 일부만 더해서 30이 나옴
2. 30 = 3 + 6 + 9 + 12... 잠깐, 12는 없으니 1~10 범위
3. 3 + 6 + 9 = 18 (X)
4. 2 + 4 + 6 + 8 + 10 = 30 (✓ 짝수의 합!)
5. 빈칸: `i % 2 == 0`

### 흔한 빈칸 유형

| 빈칸 위치 | 흔한 답 |
|---|---|
| `range(?, ?)` | `1, n+1` 또는 `0, n` |
| `if ?:` | `i % 2 == 0`, `i > 0`, `i % 3 == 0` 등 |
| `total += ?` | `i`, `nums[i]`, `1`(카운트일 때) |
| `for ? in ?` | `i in range(...)`, `x in nums` |

---

## 7.4 디버깅 — 자주 나오는 버그 7종 ⚠️

코드에 의도와 다른 부분이 있고, 그걸 찾아 고치는 문제.

### 버그 1. 들여쓰기 오류

```python
for i in range(5):
    print(i)
print(i * 2)
```

> `print(i * 2)`가 for 안에 있어야 하는데 밖에 있음.
> **고치는 법**: `print(i * 2)`를 4칸 들여쓰기.

### 버그 2. `=` vs `==`

```python
if x = 5:
    print("five")
```

```
SyntaxError: invalid syntax
```

> **고치는 법**: `if x == 5:`로 수정.

### 버그 3. range 끝값 누락

```python
total = 0
for i in range(1, 10):
    total += i
print(total)
```

```
45
```

> 1~10의 합이 의도였는데, 10이 빠져 45가 나옴 (정답은 55).
> **고치는 법**: `range(1, 11)`.

### 버그 4. 누적 변수 초기화 위치

```python
n = 5
for i in range(1, n+1):
    total = 0
    total += i
print(total)
```

```
5
```

> 매번 `total = 0`으로 리셋되어 마지막 i값(5)만 출력.
> **고치는 법**: `total = 0`을 for 밖으로 옮긴다.

### 버그 5. input 형변환 누락

```python
a = input()
b = input()
print(a + b)
```

(입력 `5`, `3`)
```
53
```

> 문자열끼리 이어붙이기가 됨.
> **고치는 법**: `int(input())`로 형변환.

### 버그 6. 인덱스 범위 초과

```python
nums = [1, 2, 3]
print(nums[3])
```

```
IndexError: list index out of range
```

> 인덱스는 0,1,2까지만 있음.
> **고치는 법**: `nums[2]` 또는 `nums[-1]`.

### 버그 7. while 변수 갱신 누락

```python
i = 1
while i <= 5:
    print(i)
```

```
1
1
1
... (무한루프)
```

> **고치는 법**: while 안에 `i += 1` 추가.

---

## 7.5 시간 안에 완성하는 사고 순서 (코드 작성 실전)

### 시간 배분 전략 (예: 30분 시험)
- **0~3분**: 문제 전체 훑어보기, 쉬운 것부터 표시
- **3~5분/문제**: 한 문제씩 풀이
- **마지막 3분**: 못 푼 문제 부분 점수라도 작성, 제출 확인

### 막혀도 0점은 피하는 법

문제를 못 풀어도 **틀이라도 적자**:

```python
n = int(input())
print(n)
```

> 일단 입력 받기와 입력값 출력만 적어도 부분 점수가 될 수 있음.

---

## 7.6 종합 풀이 흐름도

```
[문제 받기]
    ↓
[1. 문제 읽기]
    ↓
[2. 입력값 / 출력값 확인]
    ↓
[3. input()으로 입력 받기]
    ↓
[4. 출력값을 만드는 로직 구현]
    ↓
[5. print()로 출력]
    ↓
[제출]
```

---

## 7.7 시험 직전 체크리스트

- [ ] 7대 유형 템플릿 (Part 8) 머릿속에 있음
- [ ] 형변환(`int(input())`) 자동으로 나옴
- [ ] 누적 패턴 4가지(합/카운트/최대/최소) 손으로 칠 수 있음
- [ ] 들여쓰기 일관성 의식
- [ ] `=`와 `==` 헷갈리지 않음
- [ ] 막히면 일단 입력 받기까지는 적는다는 마음가짐
